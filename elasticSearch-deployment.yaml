apiVersion: apps/v1
kind: Deployment
metadata:
  name: elasticSearch-deployment
  labels:
    app: elasticSearch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: elasticSearch
  template:
    metadata:
      labels:
        app: elasticSearch
    spec:
      containers:
      - name: elasticSearch
        image: docker.elastic.co/elasticsearch/elasticsearch:8.9.0
        resources:
          limits:
            memory: "2Gi"
            cpu: "1"
          requests:
            memory: "1Gi"
            cpu: "500m"
        ports:
        - containerPort: 9200
        env:
        - name: discovery.type
          value: "single-node"
